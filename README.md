# Multi-Object-Detection-
A voice-controlled real-time object detection system that plays an intro, listens for a “start” command, then uses a YOLO model via webcam to detect objects. It highlights them on screen and announces detections through speech for an interactive experience.
